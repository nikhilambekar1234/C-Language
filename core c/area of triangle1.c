#include<stdio.h>
void main()
{
	float l,h,area;
	printf("enter dimention of length:");
	scanf("%f",&l);
	printf("enter dimention of height:");
	scanf("%f",&h);
	area = 0.5*l*h;
	printf("Area of triangle = %f",area);
	
}
