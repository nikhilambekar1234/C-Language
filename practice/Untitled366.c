#include<stdio.h>
#include<string.h>
enum weekday {1=Sunday,2,3, 4,5,6,7};
int main()
{
	int i;
	char 1=[10]=Sunday;
	enum weekday w;
	w=1;
	for(i=Sunday;i<=Saturday;i++)
	{
		printf("%s\n",i);
	}
	
}
